<h1>Taco time!!</h1>

<h2>Tacos that need to be devoured!</h2>
<div class="row">
    <div class="col-6">
      <div class="needBelly">
        <h2>EAT A TACO DANGIT</h2>
<ul>
  {{#each tacos}}
    {{#unless devoured}}
      {{> tacos/taco-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<div class="row">
    <div class="col-6">
      <div class="nommed">
<h2>Tacos that have been devoured and enjoyed!</h2>

<ul>
  {{#each tacos}}
    {{#if devoured}}
      {{> tacos/taco-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Taco</h2>
<form class="create-form">

  <div class="form-group">
    <label for="userTacoName">Taco Name:</label>
    <input type="text" id="userTacoName" name="name">
  </div>

  <button type="submit">Add Taco</button>
</form>
