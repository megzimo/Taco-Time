<h1>TACOTIME!</h1>

<h2>Tacos that need a belly!</h2>

<ul>
  {{#each cats}}
    {{#unless sleepy}}
      {{> cats/cat-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Tacos in search of a belly</h2>

<ul>
  {{#each tacos}}
    {{#if eaten}}
      {{> tacos/taco-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Taco</h2>
<form class="create-form">

  <div class="form-group">
    <label for="TortillaSandy">Taco Name:</label>
    <input type="text" id="TortillaSandy" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Taco Time??</label><br>
    <input type="radio" name="devoured" value="1" checked> Devoured!<br>
    <input type="radio" name="devoured" value="0"> In need of belly!
  </div>

  <button type="submit">Add Taco</button>
</form>